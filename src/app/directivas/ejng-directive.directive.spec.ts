import { EjngDirectiveDirective } from './ejng-directive.directive';

describe('EjngDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EjngDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
